# SmartUML Backend Environment Variables Example

# Google Gemini API Key (Required for UML diagram generation)
GEMINI_API_KEY=your_gemini_api_key_here

# Allowed origins for CORS (comma-separated list of domains or * for all)
# Example for production: ALLOWED_ORIGINS=https://smartuml.vercel.app
ALLOWED_ORIGINS=*
